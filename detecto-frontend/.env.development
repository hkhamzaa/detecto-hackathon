# Hackathon demo: talk to the real API and live alert stream, and show the
# video-upload UI instead of box pairing / RTSP / fleet health.
VITE_DEMO_MODE=true
VITE_USE_MOCKS=false
VITE_LIVE_ALERTS=true
