/**
 * The permission-scoped area — the surfaces a member reaches through a grant
 * rather than a role. One module, one chunk, same reasoning as `admin.ts`.
 */
export { default as AlertQueue } from '@/pages/alerts'
export { default as AlertDetail } from '@/pages/alerts/detail'
export { default as Cameras } from '@/pages/cameras'
